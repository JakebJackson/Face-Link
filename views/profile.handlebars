<main>
    <section id="userinfo" class="card">
        <div class="card-body">
            <h2>Username: {{user.username}}</h2>

            <span id="user-email">Email: {{user.email}}</span>
        </div>
    </section>
</main>